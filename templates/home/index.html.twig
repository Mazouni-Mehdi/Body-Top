{% extends 'base.html.twig' %}


{% block body %}
  <div class="jumbotron">
    <h1 class="display-4">Bienvenue sur le site de Body Top</h1>
    <p class="lead">Controller vos droits d’accès, des differents module souscrit a la marque Body Top</p>
    <hr class="my-4">
    {% if not app.user %}
      <p>VOUS DEVEZ VOUS CONNECTER POUR ACCEDER A VOTRE COMPTE</p>
      <p class="lead text-center d-flex align-items-center justify-content-center">
        <a class="btn btn-lg mx-1" href="{{ path('app_login') }}" role="button">Se connecter</a>
      </p>
    {% endif %}
  </div>
{% endblock %}
